# --- Core (required) ---
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
APScheduler>=3.10
pydantic>=2.6
PyYAML>=6.0
httpx>=0.27

# --- Auth (TOTP / Google Authenticator login) ---
pyotp>=2.9
qrcode>=7.4          # QR 코드(SVG) 생성, Pillow 불필요
itsdangerous>=2.1    # 서명된 세션 쿠키 (Starlette SessionMiddleware)

# --- Internet speed test ---
# Pure-python Speedtest.net client. The official Ookla `speedtest` binary is
# also supported automatically when present (see config: speedtest.method).
speedtest-cli>=2.1

# --- Optional ---
# Faster native ICMP (otherwise the system `ping` binary is used, no root needed):
#   pip install icmplib
# Router traffic statistics over SNMP:
#   pip install pysnmp-lextudio
