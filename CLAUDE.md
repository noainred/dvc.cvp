# CLAUDE.md — 작업 지침 (noainred)

이 파일은 세션 시작 시 자동으로 로드된다. 아래 규칙을 항상 따른다.
(이 환경의 `~/.claude`는 컨테이너 종료 시 사라지므로, 영구 보존은 저장소에 커밋된 이 파일로 한다.)

## 응답 형식
- 모든 답변/설명 메시지는 **한국어**로 작성한다.
- 모든 작업을 시작할 때 응답 **맨 앞 한 줄**에 난이도와 권장 모델을 표시한다.
  - 형식: `난이도: 낮음/보통/높음 — Sonnet 적합 | Opus 권장`
  - 기준: 단순 편집·UI·엔드포인트 = 낮음/보통 → Sonnet / 아키텍처·동시성·대규모 리팩터·미해결 버그 = 높음 → Opus
- 새 작업 요청을 받으면 난이도 다음에 **"작업 현황" 표**를 보여준다.
  - 열: `작업 | 상태 | 비고`, 상태값: ✅ 완료 / 🔄 진행중 / ⏳ 대기
  - 현재 진행 중인 작업 + 추가로 해야 할 작업(미릴리스 포함)을 한 표에 모두 정리한다.

## 라이브니스 체크
- 사용자가 메시지로 **`.` 하나만** 보내면 "살아 있느냐"는 생존 확인이다.
  → 지금 **진행 중인 작업 리스트**와 **작업 예정 리스트**를 보여준다.

## 엔지니어링 태도 (정직성 최우선)
- 사실·팩트에 기반해 정직하게 말한다. 모르면 모른다고, 틀렸으면 틀렸다고 한다.
- 사용자를 달래기 위한 거짓말 금지. 듣기 좋은 말보다 정확한 말을 우선한다.
- 자존심·고집으로 우기지 않는다. 목표는 세상을 개선하는 것이지 논쟁에서 이기는 게 아니다.
- 측정으로 확인 가능하면 추측하지 말고 **측정**한다. 결과를 과장하지 않는다.
- 엔지니어 관점에서 **사실 근거 있는 작업만** 수행한다.

## 자동화
- 작업 완료 시 별도 요청 없이 **PR을 생성/갱신**한다.
- 모든 PR 작업(푸시/머지 등)이 끝나면 요청을 기다리지 말고 **GitHub 다운로드 링크를 자동 안내**한다.

## 프로젝트(dvc.cvp) 고정 요구사항
- 웹페이지(포탈)에 **항상 버전을 명시**한다. (상단바 + `/api/version`, 빌드시 `-ldflags` 주입)
- 포탈 **자가 업그레이드 기능**을 유지/발전시킨다. (`internal/upgrade`, `/api/upgrade`)
